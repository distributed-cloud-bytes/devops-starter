-- Example: copy to init-schemas.sql and mount in compose if you need multiple schemas.
-- Not applied by default.

-- CREATE SCHEMA IF NOT EXISTS app AUTHORIZATION platform;
-- GRANT ALL PRIVILEGES ON SCHEMA app TO platform;
